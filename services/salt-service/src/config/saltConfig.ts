import { env } from './env.js';

export const saltConfig = {
  // Placeholder for Salt SDK configuration
  // Add real SDK config when integrating with Salt
  pearServiceUrl: env.PEAR_SERVICE_URL,
};
